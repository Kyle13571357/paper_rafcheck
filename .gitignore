# source PDFs are copyrighted conference papers -- not redistributed via git
(1)paper/
papers/*.pdf

# derived artifacts, regenerable from source PDFs via the pipeline
# (eval/eval_set.jsonl is hand-curated -- keep it)
blocks.jsonl
blocks/
index/
pages/
tables/
*.png

# run logs and model output; quality_report.json and vision_report.jsonl are
# regenerated by the pipeline and would otherwise churn on every run
*.log
quality_report.json
vision_report.jsonl

.DS_Store
.claude/
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
